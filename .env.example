# Discord Bot Token
DISCORD_TOKEN=YOUR_DISCORD_BOT_TOKEN

# Discord Channel ID where to post the bonus codes
CHANNEL_ID=YOUR_CHANNEL_ID

# Discord Role ID to ping (optional)
PING_ROLE_ID=YOUR_ROLE_ID

# Telegram API credentials (get them from https://my.telegram.org)
TG_API_ID=YOUR_TELEGRAM_API_ID
TG_API_HASH=YOUR_TELEGRAM_API_HASH

# Telegram channels to monitor (comma-separated, can be usernames or IDs)
# Supports two types of channels:
#   - Channels with code in spoiler (like @StakecomDailyDrops)
#   - Channels with separate messages: announcement → code (like @RainsTEAM)
# Example: @StakecomDailyDrops,@RainsTEAM,-1001234567890
TG_CHANNELS=

# Telegram string session (will be generated on first run)
TG_STRING_SESSION=

# Optional: Button label text (default: 🎁 Lien du bonus)
BUTTON_LABEL_TEXT=🎁 Lien du bonus

# Optional: Image URL for the embed (default: provided)
BONUS_IMAGE_URL=https://cdn.discordapp.com/attachments/1290178652327252009/1413168626780995704/Logo_Bonus_v2.png

# Optional: Enable debug mode for Telegram (0 or 1)
DEBUG_TELEGRAM=0

# Optional: Send a health ping on startup (0 or 1)
TG_HEALTH_PING=0
